@model IEnumerable<OnlineBookstore.Models.Order>
@{
    ViewData["Title"] = "إدارة الطلبات";
}

<div class="container py-4">
    <div class="row align-items-center mb-4">
        <div class="col-md-6">
            <h1 class="fw-bold mb-0"><i class="bi bi-bag-check text-primary me-2"></i>إدارة الطلبات</h1>
        </div>
        <div class="col-md-6 text-md-end mt-3 mt-md-0">
            <a asp-action="Dashboard" class="btn btn-outline-primary">
                <i class="bi bi-arrow-right me-2"></i>العودة للوحة التحكم
            </a>
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>رقم الطلب</th>
                            <th>العميل</th>
                            <th>التاريخ</th>
                            <th>الإجمالي</th>
                            <th>طريقة الدفع</th>
                            <th>الحالة</th>
                            <th>الإجراءات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var order in Model)
                        {
                            <tr>
                                <td class="fw-bold">#@order.OrderId</td>
                                <td>@(order.User?.FullName ?? "غير معروف")</td>
                                <td>@order.OrderDate.ToString("MMM dd, yyyy HH:mm")</td>
                                <td class="fw-bold text-primary">@order.TotalPrice.ToString("F2") $</td>
                                <td>
                                    @(order.PaymentMethod == "Credit Card" ? "بطاقة ائتمان" :
                                      order.PaymentMethod == "Debit Card" ? "بطاقة خصم مباشر" :
                                      order.PaymentMethod == "PayPal" ? "باي بال" :
                                      order.PaymentMethod == "Cash on Delivery" ? "الدفع عند الاستلام" :
                                      order.PaymentMethod)
                                </td>
                                <td>
                                    <span class="badge @(order.Status == "Completed" || order.Status == "Delivered" ? "bg-success" : order.Status == "Pending" ? "bg-warning text-dark" : "bg-info")">
                                        @(order.Status == "Completed" ? "مكتمل" :
                                          order.Status == "Delivered" ? "تم التوصيل" :
                                          order.Status == "Pending" ? "قيد الانتظار" :
                                          order.Status == "Processing" ? "قيد المعالجة" :
                                          order.Status == "Cancelled" ? "ملغي" :
                                          order.Status)
                                    </span>
                                </td>
                                <td>
                                    <div class="btn-group">
                                        <a asp-controller="Orders" asp-action="Details" asp-route-id="@order.OrderId" class="btn btn-sm btn-outline-primary" title="عرض التفاصيل">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        @if (order.Status == "Pending")
                                        {
                                            <form asp-controller="Orders" asp-action="UpdateStatus" method="post" class="d-inline">
                                                <input type="hidden" name="orderId" value="@order.OrderId" />
                                                <input type="hidden" name="status" value="Processing" />
                                                <button type="submit" class="btn btn-sm btn-outline-warning" title="بدء المعالجة">
                                                    <i class="bi bi-arrow-left"></i> @* أيقونة تشير لليسار للدلالة على التقدم للخطوة التالية في RTL *@
                                                </button>
                                            </form>
                                        }
                                        else if (order.Status == "Processing")
                                        {
                                            <form asp-controller="Orders" asp-action="UpdateStatus" method="post" class="d-inline">
                                                <input type="hidden" name="orderId" value="@order.OrderId" />
                                                <input type="hidden" name="status" value="Completed" />
                                                <button type="submit" class="btn btn-sm btn-outline-success" title="إتمام الطلب">
                                                    <i class="bi bi-check-lg"></i>
                                                </button>
                                            </form>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>