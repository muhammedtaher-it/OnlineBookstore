@model List<OrderViewModel>
@{
    ViewData["Title"] = "إدارة الطلبات";
}

<div class="container py-4">
    <h1 class="fw-bold mb-4"><i class="bi bi-bag-check text-primary me-2"></i>إدارة الطلبات</h1>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>رقم الطلب</th>
                            <th>العميل</th>
                            <th>التاريخ</th>
                            <th>المنتجات</th>
                            <th>الإجمالي</th>
                            <th>الحالة</th>
                            <th>الإجراءات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var order in Model)
                        {
                            <tr>
                                <td class="fw-bold">#@order.OrderId</td>
                                <td>@(order.ShippingCity)، @(order.ShippingCountry)</td>
                                <td>@order.OrderDate.ToString("MMM dd, yyyy")</td>
                                <td>@order.OrderDetails.Count</td>
                                <td class="fw-bold text-primary">@order.TotalPrice.ToString("F2") $</td>
                                <td>
                                    <span class="badge @(order.Status == "Completed" || order.Status == "Delivered" ? "bg-success" : order.Status == "Pending" ? "bg-warning text-dark" : "bg-info")">
                                        @(order.Status == "Completed" ? "مكتمل" :
                                          order.Status == "Delivered" ? "تم التوصيل" :
                                          order.Status == "Pending" ? "قيد الانتظار" :
                                          order.Status == "Processing" ? "قيد المعالجة" :
                                          order.Status == "Cancelled" ? "ملغي" :
                                          order.Status)
                                    </span>
                                </td>
                                <td>
                                    <div class="btn-group">
                                        <a asp-action="Details" asp-route-id="@order.OrderId" class="btn btn-sm btn-outline-primary" title="عرض التفاصيل">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        @if (order.Status == "Pending")
                                        {
                                            <form asp-action="UpdateStatus" method="post" class="d-inline">
                                                <input type="hidden" name="orderId" value="@order.OrderId" />
                                                <input type="hidden" name="status" value="Processing" />
                                                <button type="submit" class="btn btn-sm btn-outline-warning" title="بدء المعالجة">
                                                    <i class="bi bi-arrow-left"></i>
                                                </button>
                                            </form>
                                        }
                                        else if (order.Status == "Processing")
                                        {
                                            <form asp-action="UpdateStatus" method="post" class="d-inline">
                                                <input type="hidden" name="orderId" value="@order.OrderId" />
                                                <input type="hidden" name="status" value="Completed" />
                                                <button type="submit" class="btn btn-sm btn-outline-success" title="إتمام الطلب">
                                                    <i class="bi bi-check-lg"></i>
                                                </button>
                                            </form>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>